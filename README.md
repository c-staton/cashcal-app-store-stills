# Cashcal App Store stills

Public marketing folder lives here (images, not this GitHub repo):

**https://cashcal-app-store-stills.fly.dev/**

Includes listing stills (`store-regen/` 01 home, then 02 calendar, then 04 bills), `sim-marketing/` aliases, `app-icon-1024.png`, `reference/ORDER.md`, and `README.md`.

`qa-internal/` engineering QA shots are not in the public folder.

This GitHub repo is a pointer only. The Cursor GitHub App can create the repo but cannot git-push binary stills into it, so the actual pack is hosted on Fly.
